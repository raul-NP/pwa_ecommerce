SUBIR A PRODUCCION:
    - Instalar cludfyre
    - cloudflared tunnel --protocol http2 --url http://localhost:5000
    - Copiar url en el .env del front
    - docker compose build panel
    - docker compose up -d panel
    - cloudflared tunnel --protocol http2 --url http://localhost:3000